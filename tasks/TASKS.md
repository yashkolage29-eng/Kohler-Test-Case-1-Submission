# TASKS — KOHLER AI Bathroom Designer & Planner

This file is the authoritative execution state for implementation. The orchestrator owns
status changes and must not mark a task `DONE` without evidence from its acceptance criteria
and verification method.

Statuses: `PENDING` | `IN PROGRESS` | `BLOCKED` | `REVIEW` | `DONE`

## Decision and setup gates

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-000 | DONE | Resolve pre-implementation contract gates | Record the product/architecture decisions needed before implementation: distinguish the normal recommendation “3 ways” route from impossible-brief relaxation, define the user-visible acceptance contract for each, and confirm the missing root guidance-file situation is intentionally handled through `.clinerules/`. Do not silently rewrite accepted ADRs. | orchestrator | — | Decision record names the two flows, their inputs/outputs, MVP status, and test obligations; no unresolved contradiction is hidden in later tasks. | Product/architecture review against PRD, OPT, SYS-ARCH, and ADR-005/006/015/018; user confirmation where required. | P0 |
| T-001 | DONE | Scaffold the workspaces | Create the minimal npm-workspaces monorepo structure for `engine`, `web`, and `server`, with TypeScript/Vite/Vitest configuration compatible with the documented offline run path. | architecture-agent | T-000 | `npm install` and the documented local commands have deterministic workspace entry points; packages remain engine/web/server separated. | `npm install`, TypeScript build, Vitest smoke command, and package-boundary review. | P0 |
| T-002 | DONE | Define shared contracts and deterministic config | Establish the shared TypeScript types for `InputSet`, `Candidate`, `Plan`, `BuildOutput`, receipts, render geometry, rule verdicts, priorities, and configuration. Define canonical ordering, numeric precision, and hash inputs without introducing UI or network dependencies into engine types. | architecture-agent | T-001 | Engine, web, and server compile against one explicit contract; config owns rule values, anchors, weights, slot size, and search budgets. | Cross-workspace `tsc`; contract fixture compilation; architecture review against SYS-ARCH §§5–8 and OPT §§2–3/14. | P0 |

## Deterministic engine foundation

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-003 | DONE | Implement catalog schema and quarantine pipeline | Implement catalog types, strict load-time validation, geometry-bounding-box checks, deterministic quarantine records, canonicalization, catalog hashing, and load reports. | backend-agent | T-001, T-002 | Invalid rows are quarantined rather than crashing or entering search; surviving rows and data gaps are deterministic and receipt-ready. | Catalog quarantine matrix, geometry-bbox, duplicate, reference-integrity, canonical-hash, and fixpoint tests. | P0 |
| T-004 | DONE | Curate the demo catalog | Author the real KOHLER catalog data and finishes to the schema’s target coverage, including water metadata, finish prices, descriptors, compatibility references, and substitutions. Verify public model numbers, prices, and dimensions by hand. | product-agent | T-003 | 50–64 valid SKUs load with zero quarantine; class/feature/finish coverage targets and honesty metadata are met. | `loadCatalog()` report, curation checklist, stable snapshot ID, and product-agent acceptance report. | P0 |
| T-005 | DONE | Implement room and fixture geometry primitives | Implement normalized rectangle/L-shape room representation, wall strips, openings/keep-clear regions, slot-grid projection, fixture footprints, AABBs, and deterministic coordinate normalization. | geometry-agent | T-002 | Confirmed room inputs produce canonical geometry; malformed/unconfirmed inputs are rejected at the gate; adjacent-wall and L-shape geometry is represented. | Unit tests for normalization, wall strips, openings, slot rounding, degenerate inputs, and reordered-input determinism. | P0 |
| T-006 | DONE | Implement hard geometry rules C1–C6 | Implement fit/overlap, working clearances, door swings, zones, plumbing minima, and layout sanity as structured rule evaluators with measured trace data. Keep all numeric values in config. | geometry-agent | T-005 | Every verdict includes rule ID, values used, measured deltas, explanation, and pass/fail; no invalid placement can pass the geometry layer. | Pass/fail/boundary tests per rule, property tests for collision/clearance/swing invariants, and tiny/L-shape cases. | P0 |
| T-007 | DONE | Build compatibility graph and C7 validation | Implement default compatibility, force/veto precedence, finish harmony, feature-constraint matching, mounting consequences, symmetric graph closure, and substitution sets. | backend-agent | T-003, T-004, T-006 | Every bound SKU pair satisfies C7; quarantined/dangling/self-referenced products cannot enter a candidate; substitutes remain explicit relaxation inputs. | Override-precedence, finish-harmony, feature-match, mounting, substitute-closure, and no-quarantined-reference tests. | P0 |

## Search, scoring, and optimization

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-008 | DONE | Implement archetype and constructive solver | Implement archetype filtering, class-count constraints, cost-aware SKU binding, deterministic placement ordering, forward checking, bounded backtracking, and candidate assembly. | backend-agent | T-004, T-005, T-006, T-007 | Solver emits only candidates that can reach full validation; search is bounded, deterministic, and never hangs; step-one confirmation is enforced. | Solver fixtures, invalid-candidate rejection, backtracking-budget, ordering/hash, and typical-room latency tests. | P0 |
| T-009 | DONE | Implement objective, BOM, ranking, and receipt | Implement anchored utilities, priority/spaciousness weights, deterministic tie-breaks, finish-aware BOM pricing, budget summaries, top-k ranking, and decision receipts. | backend-agent | T-002, T-008 | `solve()` returns a validated costed plan or a typed infeasibility result; cost never exceeds `B_max`; every user-facing fact can trace to the receipt. | Objective oracle, anchor stability, tie-break, BOM total, budget-edge, top-k, and receipt completeness tests. | P0 |
| T-010 | DONE | Implement explicit relaxation and “3 ways” behavior | Implement two explicit modes: ordinary valid briefs may expose deterministic alternative priority profiles as a stretch route; impossible briefs must expose independently re-searched and re-validated relaxation paths as the MVP route. Include fail-cause tracing, minimum-gap reporting, ordered relaxation paths, and distinct-plan checks. | backend-agent | T-000, T-009 | The two modes have separate typed outputs and UI labels; every surfaced alternative is valid and labeled by measured changes; exhaustion returns honest out-of-scope with minimum viable cost when available. | Relaxation minimality, distinctness, 1–3 result bounds, impossible brief, ordinary-profile alternatives, out-of-scope, and full-validator-on-every-output tests. | P0 |
| T-011 | DONE | Implement cache and incremental re-optimization | Add canonical cache keys, weight-independent candidate utilities, invalidation, pure re-score changes, local replacement, and full re-search for global changes. | backend-agent | T-009, T-010 | Priority/spaciousness changes re-score without stale geometry; local/global changes invalidate correctly; all final outputs are fully revalidated. | Cache-key/reordering, invalidation, re-score equivalence, local-edit fallback, and latency regression tests. | P1 |

## Server, AI, and presentation

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-012 | DONE | Implement thin server and static host | Implement built-asset hosting, SPA fallback, bounded request parsing, path allowlisting, and the documented server error behavior without moving the engine solve into a backend round-trip. | backend-agent | T-001, T-002 | Static app runs locally; invalid paths/body sizes are rejected safely; core solving remains available without the server/network. | Server integration, path traversal, body-limit, SPA fallback, and offline-start tests. | P1 |
| T-013 | DONE | Implement AI adapter and offline fallback | Implement receipt-bound narration, taste-to-feature mapping, photo proposal handling, relaxation phrasing, NIM proxy integration, timeouts, schema validation, and deterministic fallback behavior. | backend-agent | T-002, T-009, T-012 | AI output cannot become geometry/compatibility/cost authority; offline mode completes the core demo; secrets never reach the web bundle. | Adapter contract, malformed/timeout fallback, receipt-binding, prompt-injection, image-size, and no-network tests. | P1 |
| T-014 | DONE | Implement 2D layout renderer | Render the authoritative room, openings, fixture AABBs, dimensions, clearances, and swing annotations from engine geometry and trace data. | frontend-agent | T-002, T-005, T-006, T-009 | Canvas output is a direct projection of engine geometry; no renderer-side fixture geometry is invented. | Render-equality fixtures, annotation snapshots/smoke checks, and boundary-coordinate tests. | P1 |
| T-015 | DONE | Implement procedural 3D renderer | Build the room and fixture scene from catalog `geometry_descriptor` primitives, chosen finishes, solver placement, and camera controls. Overhauled with MeshPhysicalMaterial PBR finishes (chrome, brushed_nickel, matte_black, brushed_gold, stone, white), rich lighting (warm key + cool fill + hemisphere + ceiling lamp, boosted 2-3x), **fog removed** (was FogExp2 0.0003 causing dusty/hazy look), decor items (towels, plants, rugs), free OrbitControls with damping, ACES filmic tone mapping, and tile/wall canvas textures. **AI decor pipeline**: user taste → NIM/offline generates `ConceptDraft` with fixtures + `AIDecorItem[]` (rug, plant, mirror, towel, lamp, art, vase) → parsed and rendered in both concept panel and main 3D view. Catalog evidence fixed (unverified→verified) so authored SKUs load. | frontend-agent | T-002, T-004, T-009 | Every visible fixture maps to a BOM/catalog descriptor; changing a descriptor changes the rendered primitive; no photoreal asset dependency; authored SKUs load into catalog; AI decor renders from taste description. | Scene graph equality checks, nonblank canvas smoke test, finish mapping checks, desktop visual inspection, 67/67 web tests green. | P1 |
| T-016 | DONE | Implement room and taste flow | Build the store and Room/Taste screens: photo/rectangle-L input, authoritative dimensions, preview confirmation gate, taste text, feature constraints, priority, spaciousness, budget, and explicit loading/empty/error states. | frontend-agent | T-009, T-012, T-013 | Solver cannot run before confirmation; user edits remain typed and deterministic; offline AI fallback is visible without changing correctness. | UI smoke flow, gate-blocked state, malformed-input state, and offline fallback checks. | P1 |
| T-017 | DONE | Implement results, adjustment, and export flow | Build plan/BOM presentation, 2D/3D toggle, receipt panel, narration, relaxation choices, priority/budget/fixture/door changes, and BOM/layout export. | frontend-agent | T-010, T-011, T-014, T-015, T-016 | Result view only shows completed validated outputs; impossible briefs offer the agreed alternatives; adjustment and export preserve receipt/BOM/render consistency. | End-to-end UI smoke, re-optimization flow, relaxation selection, export artifact, and responsive review. | P1 |

## Integration and release gates

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-018 | DONE | Integrate the offline end-to-end path | Wire engine, server, AI fallback, store, renderers, receipt, and export into the documented Room → Taste → Result → Adjust → Export journey. | architecture-agent | T-011, T-013, T-017 | A clean checkout runs the core loop with no network/key; package boundaries and typed contracts remain intact. | Clean install/build/run, end-to-end happy path, offline path, and architecture review. | P0 |
| T-019 | DONE | Run adversarial engine QA | Attack geometry, catalog, budget, compatibility, determinism, relaxation, cache, and performance invariants with property/regression tests. | qa-agent | T-008, T-009, T-010, T-011 | No invalid plan appears on any output path; every discovered defect has a regression test and owner. | Full Vitest suite, adversarial suite, exact failure reports, and severity triage. | P0 |
| T-020 | DONE | Run server/UI/security QA | Test server boundaries, AI failure/degradation, UI state completeness, render equality, export, secrets, path traversal, input limits, and cross-viewport behavior. | qa-agent | T-012, T-013, T-017, T-018 | No critical/high findings remain; offline and hosted-AI paths behave consistently for correctness. | Integration/security checks, UI smoke, render checks, performance measurement, and QA report. | P0 |
| T-021 | DONE | Demo hardening and acceptance rehearsal | Exercise the scripted typical brief and impossible brief, verify narration/receipt honesty, capture final reproducibility and latency evidence, and prepare the demo rails. Rehearsal harness `scripts/rehearse.mjs`; evidence + demo rails in `submission/DEMO_REHEARSAL.md`. Closed the T-020 `/api/nim` GET 405 guard. Menu-richness hunt (TODO-27) concluded: 1 honest plan is the ceiling for demo-reachable briefs; causes recorded in TODO.md. | product-agent | T-018, T-019, T-020 | Demo survives the judge-machine scenario, shows the buildability thesis clearly, and meets the MVP acceptance criteria without hidden manual steps. | `npm run build && npm test` (320/320 green), `node scripts/rehearse.mjs` (deterministic typical plan ₹40,200 in ≤200 ms; drop-class and swap-sku menus validated; kill brief = honest out-of-scope in 7 ms), offline server boot HTTP 200 at 127.0.0.1:4173, GET /api/nim → 405. | P0 |
| T-022 | DONE | Prepare submission evidence | Assemble the AI prompts documentation source, implementation evidence, demo checklist, and any required deck/video inputs without changing product scope. Development log completed with fact-checked entries for T-000…T-022 plus a verbatim product-prompt appendix (from `packages/server/src/ai/adapter.ts`); `submission/SUBMISSION_CHECKLIST.md` created with the 4-deliverable review, deck outline, video shot script, live re-verification, and honest limitations (T-015 visual gate remains open; no git repo). No product-scope change. | product-agent | T-021 | Submission artifacts accurately reflect actual prompts, decisions, failures, and verification; no fabricated history or claims. | Submission checklist review against PRD deliverables and `submission/AI_DEVELOPMENT_LOG.md`; live re-run 2026-09-16: `npm run build` clean, `npm test` 320/320, `node scripts/rehearse.mjs` (₹40,200 deterministic plan, 193 ms; drop-class/swap-sku menus). | P1 |

## Approved AI editing scope

| ID | Status | Name | Description | Agent | Dependencies | Acceptance criteria | Verification | Priority |
|---|---|---|---|---|---|---|---|---|
| T-023 | IN PROGRESS | Review-before-Apply room proposals | Add bounded natural-language dimension/opening proposals through the existing NIM adapter and shared throttle; preserve the direct preview editor. | orchestrator | T-013, T-016 | No mutation before explicit Apply; deterministic validation, stale-response protection, undo/cancel, replacement warning; failures preserve work; no secrets in responses/logs. | Focused contract, adapter/HTTP, reducer and UI tests; web/server typecheck and build. | P1 |
| T-024 | DONE (superseded by T-025) | Session-only conceptual 3D drafts | Generate structured AI-invented procedural fixtures from confirmed room and taste, separately from catalog solver outputs. Drag-to-move editing added for fixture positioning in the 3D preview. | orchestrator | T-023 | Selection, position/quarter-turn rotation/dimension controls and removal; deterministic bounds/overlap/front-clearance/opening checks; invalid drafts remain drafts; replacement warning; no draft BOM/export/persistence or mesh generation; drag-to-move in 3D preview. | Focused geometry/state/scene/UI tests; web/server typecheck and build; scoped UI review. | P1 |
| T-025a | DONE | Décor contract, placer, offline fallback | `engine/src/decor.ts`: strict `parseDecorProposal`, deterministic `placeDecor` against `RenderGeometry`, keyword `offlineDecorProposal`. | geometry-agent | T-015 | Placed décor never intersects fixture AABBs/clearances/opening keep-clear zones; wall items on wall strips facing inward; unfit items skipped; ≤14 items, ≤3 lights; deterministic. | Engine unit tests. | P1 |
| T-025b | DONE | NIM pools, décor route, cache, vision photo | Split limiter (décor 10/min, general 25/min), multimodal + model override, `decor` route with LRU cache and offline fallback, photo → opening `RoomProposal` via `NIM_VISION_MODEL`, 10 s default timeout; remove `concept-draft`. | backend-agent | T-025a contract | Pools independent; cache hit = 0 NIM calls; failures → offline décor; photo proposals never auto-apply; no key leaks. | Server unit/HTTP tests; live probe. | P1 |
| T-025c | DONE | Auto AI décor on Result page | Remove page-2 concept draft UI/state; auto-request décor per plan+taste with cache, 12 s timeout, stale drop; placed décor + emitting lights in main 3D render; overlay pill/shimmer/staged reveal/caption; photo proposals into room review panel; specific AI error messages. | frontend-agent | T-025a, T-025b | No draft option anywhere; décor appears automatically with visible working cue; weights-only re-optimize reuses cache; offline chip on fallback. | Web tests, build, visual check. | P1 |
| T-025d | DONE | QA décor + AI paths | Adversarial tests on placement (tiny/L/large rooms, every anchor), rate-limit burst, fallback paths, UI states. | qa-agent | T-025c | Full suite green; no décor overlap; zero upstream 429 in burst. | 2026-09-18: `npm run build`/`typecheck`/`npm test` green (engine 511, server 76, web 60); live browser runs on :4173 show pill → "Styled by AI" (≈3 s) and offline fallback on NIM 503 overload; 14-request décor burst → 6 blocked locally by the 10/min pool, 0 upstream 429; photo route verified against the live vision model. | P1 |
| T-026a | DONE | Remove Decision receipt panel | Result page drops the receipt panel (AI narration, rule trace, scored candidates, catalog note) and the narration request/state; result layout becomes one column. Server `narrate` route left in place (unused by UI). | orchestrator | — | No receipt UI; no narration NIM call from the web app; tests green. | 2026-09-18: web typecheck + vitest green. | P1 |
| T-026b | DONE | Drag editing on page-1 preview | `web/src/roomDrag.ts` (pure): drag a wall to resize (joined walls), drag a door/window along a wall or onto the nearest wall, drag the L-shape inner corner. 10 mm snap; every candidate re-validated by `buildRoomPreview` + `openingError` (only newly introduced errors block); live label; one `APPLY_ROOM_DRAG` per drag = one undo step; un-confirms the room. | orchestrator | T-023 | All four drag kinds work in a browser; invalid drops revert to last valid; click-to-select still works. | 2026-09-18: 10 unit tests; Playwright run on :5173 (wall 2400→2840, depth 1800→1410, door moved wall, L cut 900→1160, undo) with no console errors. | P1 |
| T-026c | DONE | Detailed 3D room render | Procedural detail meshes inside each catalog primitive box (tub incl. freestanding/alcove, one/two-piece and wall-hung toilet, basins, vanity carcass/doors/top with basin cut-outs, faucets, shower heads, bars/rings/hooks/mirrored cabinet); procedural white-marble floor + painted walls; bundled CC0 oak/walnut textures and 2 plant GLBs (1.2 MB); window infill/casing/muntins/glass/sill, door casing + open leaf, baseboards; environment reflections; overview cutaway; eye-level "View" presets; wall colour from AI palette. ADR-030. Shower glass panels skipped (no shower-enclosure SKU in catalog). | orchestrator | T-015, T-025c | Meshes never exceed their primitive box; fallback for unknown parts/failed assets; offline. | 2026-09-18: `npm run build` clean; tests engine 511, server 76, web 73 (incl. every-SKU fit test); Playwright screenshots (overview + eye level, rectangle 3000×2400 and 3400×2800 with windows and vanity), no console errors; `.glb` static serving verified. | P1 |
| T-027a | DONE | Mounting heights + basin counter | Engine `render/mounting.ts` `mountElevationMm(sku)`: undercounter/wall-hung basin rims at 850 mm, vessel bowls and deck faucets on an 850 mm counter, shower heads/columns topping at 2100, valves at 1000, bars/rings at 1100 centre, tissue holder 650, mirrored cabinet from 1150, wall-hung toilet rim 400; floor products 0. SceneSpec adds it to catalog y. Renderer adds a presentation-only counter slab (oval cut-out for undercounter) under standalone deck basins/faucets; caption says it is not included. Surface décor no longer lands inside a standalone basin. | orchestrator | T-026c | No fixture floats or sits on the floor unless floor-standing; slab never in BOM; tests. | 2026-09-18: 4 engine + 3 web tests; Playwright renders show basin on counter. | P1 |
| T-027b | DONE | Curated style presets | Engine `styles.ts`: 8 presets (Minimalist Modern, Classic Luxury, Japanese Zen, Japanese Brutalism, Japandi, Scandinavian, Industrial Loft, Coastal) = palette/metal/light + floor/wall material + finish family + curated décor. `DecorStyle.preset` optional (strict parse); keyword detection for offline décor; AI prompt may name a preset. New décor types chandelier/lantern/bench/basket/bowl. Taste screen style cards; picking one sets its finish-family chip and skips the NIM décor call. Renderer maps floor/wall materials to bundled textures and décor types to bundled CC0 models per preset. ADR-031. | orchestrator | T-025, T-026c | Each preset places ≥4 décor items validly in rect + L rooms, deterministic; every mapped asset bundled; renders without errors. | 2026-09-18: build clean; tests engine 520, server 76, web 79; Playwright renders of all 8 presets + a typed-text brief, no console errors or failed requests. | P1 |
| T-028 | DONE | Faucet on basin + style-driven products | Solver mounts each deck faucet on its basin (same wall, centred; C1 deck-mount exemption); catalog default rule pairs vessel basins with tall faucets only; style presets declare preferred product forms (`preferredTypes`) that narrow binding pools with per-class fallback; render keeps a back deck for the faucet. ADR-032. | orchestrator | T-027a, T-027b | Faucet always inside its basin footprint on the same wall; no vessel/standard-faucet or undercounter/tall-faucet pair; presets yield different SKU sets; an unfittable preference never makes a feasible brief fail. | 2026-09-18: build/typecheck clean; tests engine 533, server 76, web 80; rehearse.mjs deterministic; Playwright on :5173 — Japandi (vessel + tall faucet), Classic Luxury (undercounter + widespread brass), Minimalist (wall-hung basin + wall-hung WC) render with faucet on the basin deck, no console errors. | P1 |
| T-028b | DONE | Result-page style switcher | "Style" dropdown in the 3D render toolbar (No style + 8 presets). Changing it applies the same state change as a taste-page style card and re-solves right away, so products, materials and décor update without leaving the Result page. | orchestrator | T-028 | One change = one re-solve; the dropdown reflects the current preset; works at phone width. | 2026-09-18: web typecheck/lint clean, 80 web tests; Playwright on :5173: None → Japandi → Classic Luxury → Coastal → None changes the BOM each time and returns to the original plan; no horizontal scroll at 390 px; no console errors. | P2 |
| T-029 | DONE | Keep openings in place on shape switch | `SET_ROOM_SHAPE` re-homes openings by physical position (`rehomeOpenings` in `web/src/store.ts`): an opening stays on whichever new wall runs through it (clamped if it overhangs an end), otherwise moves to the longest free wall that fits; unfittable ones keep their placement so `openingError` still reports them. | orchestrator | T-026b | Rectangle → L-shape no longer triggers "door-1 does not fit"; L → rectangle restores the original placement. | 2026-09-18: web typecheck/lint clean, 81 web tests (incl. round-trip test); Playwright on :4173 and :5173 — L-shape switch shows no error, door stays on the long wall, plan generates. | P1 |
| T-030 | DONE | Remove "Suggest a room edit" block | Room page no longer shows the natural-language room-edit panel (textarea + "Review AI suggestion"); its web handler/listener are removed. The same slot now renders only when a photo upload returns door/window proposals (Review → Apply / Discard). Supersedes the text-request part of T-023. | orchestrator | T-023, T-025b | No room-edit text box on page 1; photo proposals still reviewable and never auto-applied. | 2026-09-19: web typecheck clean, 81 web tests; Playwright on :4173 and :5173 — block absent, room → taste → result works, no console errors. | P2 |
| T-031 | DONE | Luxury editorial UI restyle | Web chrome restyled (ADR-033): ivory/charcoal palette with a restrained brass accent and a muted green for validated states only; bundled Cormorant Garamond (h1/h2, numerals) + Inter Variable (UI) via `@fontsource`; 2/4 px radii, hairline borders, soft shadows; KOHLER tracked-caps wordmark + serif italic "AI Bathroom Designer" (no logo mark); stepper uses 01/02/03 serif numerals; decorative checks replaced by brass hairlines; 200 ms colour transitions, no hover lift, reduced-motion respected. Room/2D preview SVG recoloured. 3D materials and copy untouched (one label: "Authoritative preview" → "Plan preview"). Also fixed the pre-existing 390 px overflow in the Adjust panel. | orchestrator | — | All three screens + relaxation state restyled; no `data-action` hooks changed; text contrast ≥ 4.5:1; no horizontal scroll at 390 px. | 2026-09-19: web typecheck clean, 81 web tests, `vite build` ok, Playwright 2/2 on :5173; before/after screenshots at 1440 and 390 px (room, preview, taste, result, relaxation); token contrast computed (muted 4.7–5.4, brass 5.0–5.7, placeholder 5.2). Pre-existing lint error in `proposals.test.ts` (unused `selectedPlan`) untouched. Contrast pass (user: "looks very light and faded"): darker ink/muted/borders/brass/green, whiter panels on a deeper ivory, stronger shadow, serif 500 → 600, disabled steps less faded; muted text now 7.0–8.6:1; tests/build/Playwright re-run green. Colour pass (user: "very monochromatic"): Taste-page "Brief status" panel removed, Taste page now full width (4-col style grid, feel/features left, sage trade-off panel right); spruce `#1F4E4A` primary + sand/sage tints + spruce KPI and export band with light-brass accents. 390 px: no horizontal scroll; tests/build/Playwright green. Spruce pass (user: "more of the spruce"): spruce topbar with ivory wordmark; spruce h1/h2/legends/links/secondary buttons/completed steps/footer; sage header bands on every panel. | P2 |
| T-032 | DONE | Settings actually steer the plan | `bindSkus` seeds one greedy SKU set per objective axis (target, premium, luxury, water, footprint, wear) before the cheapest-first enumeration; placement capped at 48 tries / 3 valid layouts per set; `u_cost` pulls toward B_target for balanced/luxury (0.5 at zero spend); shower-or-tub always required; the basin position defaults to a vanity (integrated basin) when it fits, standalone basin only for style preference or tight rooms; dropping a required fixture goes through the labeled relaxation menu. | orchestrator | T-028 | Default room: luxury ≠ value and costs more; eco u_water ≥ luxury; airy picks smaller products than compact for every priority; ₹5L plan > ₹60k plan; default plan has shower/tub; presets differ across priorities; existing tests green. | Engine tests + full suite. | P0 |
| T-033 | DONE (via T-042) | Catalog variety (real KOHLER India SKUs only) | Add verified SKUs only where T-032 tests show a class is too thin; evidence records with source URLs; unconfirmed fields marked unverified. | orchestrator | T-032 | No invented SKUs; every addition has an evidence record. | Catalog validation tests. | P1 |
| T-034 | DONE | Nothing floats + custom-style lights | Standalone undercounter/vessel basins get a floor-standing presentation support cabinet (not in BOM, labeled); wall-hung basins get a bracket + waste pipe; custom taste lights use the detected preset, else metal (black→industrial, brass→lantern/chandelier, chrome/nickel→modern); lights labeled "décor, not in budget". | orchestrator | T-032 | No basin without visible support; custom "industrial"/"black" taste loads a non-default pendant. | Web tests + visual check. | P1 |
| T-035 | DONE | BOM shows product names | BOM rows (UI + CSV) show product name alongside model id. | orchestrator | — | Each BOM row shows name + id; CSV has a name column. | Web tests. | P2 |
| T-036 | DONE | Wanted-fixtures list | One shared list on the Taste page and the Result "Fixture mix" (replaces the single select): toilet locked on; shower, tub, accessories checkboxes (checked = include, unchecked = exclude); sink radio Auto / Vanity / Standalone basin. Engine rule becomes "at least one of shower or tub"; every template allows tub/accessories so any valid list can solve. | orchestrator | T-032 | Tub-only, shower-only, shower+tub, accessories and each sink choice solve on a roomy brief; unchecking shower and tub blocks with an inline message; list edits on the Result page mark the plan dirty and re-optimize as global. | Engine + web tests; browser check. | P1 |
| T-037 | DONE | Windows: many per wall, visible | Add window picks the largest free gap (≥ 600 mm) on any wall, centred in it; several windows per wall; windows drawn sky blue and thicker than walls. | orchestrator | — | 5000×4000 room takes ≥ 6 windows; each visible without hover; refusal only when no 600 mm gap exists. | Store tests + browser check. | P1 |
| T-038 | DONE | 10 MB photo uploads | Accept photos up to 10 MB and downscale in the browser (longest side 1600 px, JPEG) before sending; server limits unchanged. | orchestrator | — | 10 MB photo accepted; sent payload under the server's 1 MB limit; > 10 MB refused with a clear message. | Web tests. | P2 |
| T-039 | DONE | Dark themes and photo-scan fix | Add a Dark Luxury preset (granite floor, graphite paint, matte black, warm-gold accents). A dark AI palette paints walls dark and defaults the floor to granite, and a preset's own palette sets the paint colour. Photo scan accepts the vision model's echoed opening shape (nested or missing swing). | orchestrator | T-038 | A dark brief renders dark walls and floor; light presets unchanged; a real photo no longer fails with malformed output. | Engine, web and server tests; browser screenshots; one live vision call. | P2 |
| T-040 | DONE | Low budgets recover instead of failing | A pinned vanity on a ₹40–60k budget returned the out-of-scope wall. The budget deficit is now measured for the brief as chosen (rising ceilings, not one unlimited search), and dropping a pinned vanity or basin frees the other sink. | orchestrator | T-036 | Vanity pinned at ₹40–60k gives a recovery menu with a basin swap inside budget and a raise-budget option. | Engine tests. | P1 |
| T-041 | DONE | Priority tabs on the result | Replace the Priority select with Value / Balanced / Eco / Luxury tabs on the result, each a validated plan from the same candidate set with its price, difference against the selected tab and a "Same as" tag for duplicates. Render, 2D, BOM and exports follow the tab; décor style is reused across tabs (no new AI call). Default Balanced; tabs hidden on out-of-scope and shown per recovery option. | orchestrator | T-040 | Four tabs with prices; switching changes plan without re-solve; all tabs within B_max. | Engine, web tests; browser. | P1 |
| T-042 | DONE | Premium catalog | Add 11 real KOHLER India SKUs (black/French Gold faucets and rainheads, black vessel basins) with official-PDP evidence; align the luxury seed with u_luxury; one-swap size variants of the luxury seed; backtrack budget 20000. | orchestrator | T-041 | Every addition has identity, dimension and MRP evidence; default brief gives four distinct tabs; all suites pass. | Engine, web, server tests; browser. | P1 |
| T-043 | DONE | Large rooms feel furnished | Room-scaled décor (11 new presentation types incl. floor lamp, LED strip, sculpture, art; per-style fillers; 1 extra placed piece per 0.9 m² above 5 m², up to 24 items and 4 lights), AI asked for a room-scaled count, two basins first in rooms ≥ 9 m² with an Auto sink, and a style accent wall behind the wet zone. | orchestrator | T-042 | 12 m² room gets ≥ 7 extra placed pieces without collisions; small rooms unchanged; pinned sinks never doubled. | Engine, web, server tests; browser. | P1 |
| T-044 | DONE | OpenRouter provider with free-model fallback chains | NVIDIA NIM free endpoints returned 503 for every call. The OpenAI-compatible client now targets OpenRouter: provider-aware reasoning switch, 6 + 12 = 18 requests per minute (free limit 20), comma-separated model chains where only an upstream 429 moves to the next model (max 3 attempts, all counted), décor waits 25 s, markdown-fenced JSON accepted. Text: gemma-4-26b → nex-n2.5-mini → nemotron-3.5-lightning; photo: ling-3.0-flash-vl → gemma-4-26b → nex-n2.5-mini. | orchestrator | — | Live: photo, décor and taste answered through the chain. | Server tests; live calls. | P1 |

## Dependency summary

```text
T-000 → T-001 → T-002
             ├→ T-003 → T-004 ─┐
             ├→ T-005 → T-006 ─┼→ T-007 → T-008 → T-009 → T-010 → T-011
             │                  │                         ├→ T-014 → T-017
             └──────────────────┘                         ├→ T-015 → T-017
                                                          ├→ T-012 → T-013 → T-016 → T-017
                                                          └───────────────────────────────┐
                                                                                         └→ T-018 → T-019/T-020 → T-021 → T-022
```

No application feature work should begin until T-000, T-001, and T-002 are complete.
